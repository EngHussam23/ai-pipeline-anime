# 📊 Project Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        User Interface                            │
│                     (Pipeline.tsx)                               │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐   │
│  │  Upload   │  │  Classify │  │ Vectorize │  │  Extract  │   │
│  │  Button   │  │  Indicator│  │ Indicator │  │ Indicator │   │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   State Management Layer                         │
│                   (PipelineContext)                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ currentStage │  │ classification│  │ extractedData│         │
│  │   fileName   │  │   vectorData  │  │ isAnimating  │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Animation Layer                               │
│                  (Stage Components)                              │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ UploadStage  │→ │ClassifyStage │→ │VectorizeStage│         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│           │                │                 │                   │
│           ▼                ▼                 ▼                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ExtractStage  │→ │  StoreStage  │→ │  Complete    │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   UI Components Layer                            │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  ServerSVG   │  │   Particle   │  │   JsonView   │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Animation Engine                               │
│                   (Anime.js v4)                                  │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   animate    │  │   Timeline   │  │   stagger    │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow

```
User Action (File Upload)
         │
         ▼
┌────────────────────────┐
│  setCurrentStage       │
│  ('upload')            │
└───────────┬────────────┘
            │
            ▼
┌────────────────────────┐
│  UploadStage           │
│  - Render particles    │
│  - Animate flow        │
│  - Server glow         │
└───────────┬────────────┘
            │
            ▼ onComplete()
┌────────────────────────┐
│  setCurrentStage       │
│  ('classify')          │
└───────────┬────────────┘
            │
            ▼
┌────────────────────────┐
│  ClassifyStage         │
│  - Set classification  │
│  - Break particles     │
│  - Show metadata       │
└───────────┬────────────┘
            │
            ▼ onComplete()
┌────────────────────────┐
│  setCurrentStage       │
│  ('vectorize')         │
└───────────┬────────────┘
            │
            ▼
┌────────────────────────┐
│  VectorizeStage        │
│  - Set vector data     │
│  - Transform dims      │
│  - Draw connections    │
└───────────┬────────────┘
            │
            ▼ onComplete()
┌────────────────────────┐
│  setCurrentStage       │
│  ('extract')           │
└───────────┬────────────┘
            │
            ▼
┌────────────────────────┐
│  ExtractStage          │
│  - Set extracted data  │
│  - Show fields         │
│  - Data flow           │
└───────────┬────────────┘
            │
            ▼ onComplete()
┌────────────────────────┐
│  setCurrentStage       │
│  ('store')             │
└───────────┬────────────┘
            │
            ▼
┌────────────────────────┐
│  StoreStage            │
│  - DB cylinders        │
│  - Store particles     │
│  - Success pulse       │
└───────────┬────────────┘
            │
            ▼ onComplete()
┌────────────────────────┐
│  setCurrentStage       │
│  ('complete')          │
│  - Show JsonView       │
│  - Display results     │
└────────────────────────┘
```

## Component Hierarchy

```
page.tsx
  │
  └─── PipelineProvider
         │
         └─── Pipeline
                │
                ├─── StageIndicator
                │      └─── StageBadge (x5)
                │
                ├─── SvgContainer
                │      └─── SVG Canvas (800x500)
                │            │
                │            └─── Current Stage Component
                │                   │
                │                   ├─── UploadStage
                │                   │      ├─── Particle (x8)
                │                   │      ├─── ServerSVG
                │                   │      └─── Text Label
                │                   │
                │                   ├─── ClassifyStage
                │                   │      ├─── ServerSVG
                │                   │      ├─── Particles (x12)
                │                   │      ├─── Metadata Tags (x3)
                │                   │      └─── Text Label
                │                   │
                │                   ├─── VectorizeStage
                │                   │      ├─── Central Circle
                │                   │      ├─── Particles (x16)
                │                   │      ├─── Lines (x8)
                │                   │      └─── Text Labels
                │                   │
                │                   ├─── ExtractStage
                │                   │      ├─── Data Fields (x4)
                │                   │      ├─── Flow Lines
                │                   │      └─── Text Label
                │                   │
                │                   └─── StoreStage
                │                          ├─── DB Cylinders (x3)
                │                          ├─── Particles (x12)
                │                          └─── Text Label
                │
                ├─── UploadButton
                │
                └─── ResultsContainer (conditional)
                       └─── JsonView
```

## File Dependencies

```
Pipeline.tsx
  ├── imports PipelineContext
  ├── imports Stage Components
  │     ├── UploadStage.tsx
  │     │     ├── imports Particle
  │     │     ├── imports ServerSVG
  │     │     └── imports animations.ts
  │     ├── ClassifyStage.tsx
  │     │     ├── imports ServerSVG
  │     │     ├── imports animations.ts
  │     │     └── imports PipelineContext
  │     ├── VectorizeStage.tsx
  │     │     ├── imports animations.ts
  │     │     └── imports PipelineContext
  │     ├── ExtractStage.tsx
  │     │     ├── imports animations.ts
  │     │     └── imports PipelineContext
  │     └── StoreStage.tsx
  │           └── imports animations.ts
  └── imports JsonView

animations.ts
  └── imports animejs
        ├── animate
        ├── Timeline
        └── stagger
```

## Animation Timeline

```
Upload Stage (2.5s)
├─ 0.0s: Wait (500ms)
├─ 0.5s: Particles animate (1500ms)
└─ 2.0s: Server glow (500ms)

Classify Stage (3.8s)
├─ 0.0s: Set classification data
├─ 0.0s: Particles break (1000ms + 800ms)
└─ 1.8s: Metadata tags appear (750ms + 1500ms)

Vectorize Stage (6.3s)
├─ 0.0s: Loop 4 dimensions
│    ├─ Dimension transform (1000ms)
│    └─ Wait (200ms)
├─ 4.8s: Draw vector lines (1500ms)
└─ 6.3s: Complete

Extract Stage (4s)
├─ 0.0s: Set extracted data
├─ 0.0s: Animate boxes (1500ms + 500ms)
└─ 2.0s: Data flow (1500ms + 500ms)

Store Stage (4.1s)
├─ 0.0s: DB cylinders appear (1000ms + 600ms)
├─ 1.6s: Store particles (1500ms + 500ms)
└─ 3.6s: Success pulse (1000ms + 500ms)

Total Duration: ~20.7 seconds
```

## State Machine

```
┌─────────┐
│ Initial │
└────┬────┘
     │ User clicks Upload
     ▼
┌─────────┐
│ Upload  │ isAnimating: true
└────┬────┘
     │ onComplete
     ▼
┌──────────┐
│ Classify │ + classification data
└────┬─────┘
     │ onComplete
     ▼
┌───────────┐
│ Vectorize │ + vector data
└────┬──────┘
     │ onComplete
     ▼
┌─────────┐
│ Extract │ + extracted data
└────┬────┘
     │ onComplete
     ▼
┌───────┐
│ Store │
└───┬───┘
    │ onComplete
    ▼
┌──────────┐
│ Complete │ isAnimating: false
│          │ showResults: true
└──────────┘
```

## Technology Stack Layers

```
┌─────────────────────────────────────┐
│     Presentation Layer               │
│  - Styled Components                 │
│  - Tailwind CSS                      │
│  - SVG Graphics                      │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Application Layer                │
│  - React Components                  │
│  - TypeScript                        │
│  - Context API                       │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Framework Layer                  │
│  - Next.js 14                        │
│  - App Router                        │
│  - React 19                          │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Animation Layer                  │
│  - Anime.js v4                       │
│  - Custom Utilities                  │
└─────────────────────────────────────┘
```

---

**This diagram represents the complete architecture of the AI Pipeline Visualizer**
